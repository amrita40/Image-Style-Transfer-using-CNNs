# Image Style Transfer Web App

This is a Flask-based web application that applies style transfer to images.

## How to Run Locally
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
